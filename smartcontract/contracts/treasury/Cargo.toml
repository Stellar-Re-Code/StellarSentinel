[package]
name = "stellar-sentinel-treasury"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
stellar-sentinel-access-control = { path = "../access-control" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
stellar-sentinel-access-control = { path = "../access-control", features = ["testutils"] }

[features]
testutils = ["soroban-sdk/testutils", "stellar-sentinel-access-control/testutils"]
